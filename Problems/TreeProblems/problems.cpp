#include "methods.cpp"
using namespace std;

Tree* task1(Tree::Node* current, int n){
  Tree* res = new Tree();
  //tassk1(res, current, n)
  return res;
}
/*
void tassk1(Tree* res, Tree::Node* current, int n){
  if (current != nullptr){
    task1(res, current->left, n);
    if (res, current->data % n == 0){
      res->add_value(current->data);
    }
    task1(res, current->right, n);
  }
}
*/
